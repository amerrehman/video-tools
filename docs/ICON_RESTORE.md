# Icon Restore

This project restores customized folder icons by removing icon override metadata, resetting attributes, and refreshing Windows Explorer.

The repository now uses the same separated structure as Kingfisher Bot:

- application code under `src/`
- builder helpers under `src/icon_restore_builder/`
- config files under `configs/`
- backup and token scripts under `scripts/`
